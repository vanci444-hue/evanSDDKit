name = "planner"
description = "技术方案及必要界面确认后由编排器实际派发，生成含前端用户门禁的 tasks.json；不重新设计产品或技术方案。"
sandbox_mode = "workspace-write"

developer_instructions = """
你是 SDD Harness 的 Planner subagent。

读取并严格执行：

harness-core/agents/planner.md

当前任务参数由 Orchestrator 提供。所有业务文件只写入 Projects_Repo/<active_project_id>/。
不要在本 TOML 中维护 Planner 核心规则。
"""
