# 前端样式规范（default）

本件约束 default 项目的样式实现；项目视觉取值由已确认的 `docs/ui-style.md` 提供，PRD 描述功能与交互，原型展示具体页面。

## 原型参考（有原型时）

**规范说明**

- 先读取任务引用的 `docs/ui-style.md` 章节；提供原型时再读取对应页面或锚点。没有原型时按风格文档和 PRD 的页面约定实现，不以缺原型为由停止。
- **Stitch**：阅读 `docs/prototypes/NN-界面名/index.html`（及样式）中的布局与视觉。
- **Pencil**：使用 Pencil MCP 的 `batch_get`、`get_screenshot` 等查看 `docs/prototypes/*.pen`。

（无固定代码示例；以实际原型路径为准。）

---

## 设计底线

以下四节是写码时的实现要求。视觉具体值（配色/间距/圆角/字号）以已确认的风格文档为准；与原型或用户最新要求冲突时交回设计对齐。既有项目没有独立风格文档时，可复用已确认的现有设计，不凭空增加新风格。

### 设计上下文优先

- 开写页面前先找设计上下文：风格文档、已确认原型、已有组件/页面、PRD 页面描述、用户提供的截图/Logo/品牌色
- 找到上下文必须复用其中的具体值（颜色/字号/间距/圆角/组件状态），禁止凭印象改近似值
- 风格文档未覆盖的局部细节，复用项目已有设计并说明假设；影响用户结果或 AC 的缺口交回设计主 Agent

### 字阶策略

- 字号按信息层级递减，同一层级同一字号；标题与正文区分字重/字号，不得全文一套
- 判定：同一层级的信息出现两种字号 = 违规；标题与正文用同一字号同一字重 = 违规

### 圆角与视觉值

- 圆角、配色、间距按风格文档中的用途取值；未覆盖处同类组件保持一致，必要例外需有设计依据
- 判定：同一用途的取值与已确认风格不一致且无设计依据 = 违规

### 布局效率

- 数据密集场景的栅格、信息密度和响应式按项目风格及页面设计执行，不统一强制列数或对称网格
- 布局保持扁平，避免深层嵌套容器
- 判定：布局与已确认设计不符或嵌套造成溢出、遮挡等可见问题时，报告具体位置与影响

---

## 交互状态（五态）

- 本条是**实现代码的要求**，不是原型展示要求；消费方 = Developer（写码时实现，含 Mock 阶段页面代码）+ Tester（前端任务校验时核查）；原型产物不消费本条
- 所有交互组件（按钮/输入框/可点击项）必须用真实 CSS 伪类覆盖五种状态：可用（enabled，默认样式）/ 禁用（`:disabled`）/ 悬停（`:hover`）/ 按下（`:active`）/ 聚焦（`:focus-visible`）
- 判定：任一交互组件的样式表缺任一态对应的伪类定义 = 违规

---

## 布局与样式易错点

**规范说明**

- **多栏布局**：避免 `grid` / `flex` 默认等高导致某一栏内容撑高整行、破坏整体结构；工作台类页面宜固定视口高度，各栏使用 `min-height: 0` 与独立 `overflow` 滚动。
- **侧栏列表**：列表区域勿依赖会被拉伸或均分剩余高度的默认行为；列表容器应**顶部对齐**，保证从上往下稳定排布。

（无统一代码示例；按具体页面在 CSS 中落实。）
